### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String
  user: User
}

"""Date custom scalar type"""
scalar Date

input InputUserType {
  email: String!
  name: String
  password: String!
}

type Mutation {
  createDraft(content: String!, title: String!): Post
  deletePost(id: Int!): Post
  login(email: String!, password: String!): AuthPayload
  publish(id: Int!): Post
  signup(input: InputUserType!): AuthPayload
}

type Post {
  author: User
  content: String
  createdAt: Date
  id: ID
  published: Boolean
  title: String
  updatedAt: Date
}

type Query {
  drafts: [Post]
  feed: [Post]
  filterPosts(searchString: String): [Post]
  me: User
  post(id: Int!): Post
}

type User {
  email: String
  id: ID
  name: String
  posts: [Post]
}
